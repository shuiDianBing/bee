<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stynet.shuidianbing.opticalcharacterrecognition" >
    <!--android:icon="@mipmap/icon_rq_code"-->
    <application
    android:allowBackup="true"
    android:label="@string/app_name"
    android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.ScanCodeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme= "@style/ToolbarTheme">"@android:style/Theme.Light.NoTitleBar"
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".permission.PermissionActivity"/>
    </application>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />系统权限
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"/>系统权限
    <uses-feature android:name="android.hardware.camera.autofocus"/>预览模糊
</manifest>